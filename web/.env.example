# Base URL of the Go backend (repo root). No trailing slash.
# Only needed in development, where vite serves this app on its own port. In a
# production build the backend serves these files itself, so requests are
# relative and this stays unset.
# Must match FRONTEND_URL's CORS counterpart on the server side.
VITE_API_URL=http://localhost:8080
